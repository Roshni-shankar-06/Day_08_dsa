import collections

class Solution:
    def levelOrder(self, root: TreeNode | None) -> list[list[int]]:
        if not root:
            return []
        
        ans = []
        q = collections.deque([root])
        
        while q:
            curr_level = []
            for _ in range(len(q)):
                node = q.popleft()
               
